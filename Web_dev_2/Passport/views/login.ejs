<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>🔐 Login - Passport Auth</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Segoe+UI&display=swap" rel="stylesheet" />
  <style>
    :root {
      --accent: #00bfff;
      --hover: #007acc;
      --text: #e0f7fa;
      --bg-black: #000000;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background: var(--bg-black);
      color: var(--text);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, rgba(0, 191, 255, 0.1) 0%, transparent 70%);
      z-index: -1;
    }

    .card {
      background: rgba(10, 10, 10, 0.9);
      padding: 2.5rem;
      border-radius: 1rem;
      box-shadow: 0 0 20px rgba(0, 191, 255, 0.2);
      backdrop-filter: blur(6px);
      max-width: 500px;
      width: 90%;
      text-align: center;
    }

    .card img {
      width: 60px;
      margin-bottom: 1rem;
    }

    h2 {
      margin-bottom: 1.5rem;
      color: var(--accent);
    }

    label {
      display: block;
      text-align: left;
      margin: 0.5rem 0 0.25rem;
      color: var(--text);
    }

    input {
      width: 100%;
      padding: 0.6rem;
      margin-bottom: 1rem;
      border: none;
      border-radius: 5px;
      background: rgba(255, 255, 255, 0.1);
      color: var(--text);
    }

    input::placeholder {
      color: #a9cfe3;
    }

    button {
      width: 100%;
      padding: 0.75rem;
      border: none;
      border-radius: 6px;
      font-weight: bold;
      margin-top: 0.5rem;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .btn-primary {
      background-color: var(--accent);
      color: #000;
    }

    .btn-primary:hover {
      background-color: var(--hover);
      color: #fff;
      box-shadow: 0 0 12px var(--accent);
    }

    .btn-dark {
      background-color: #222;
      color: white;
    }

    .btn-dark:hover {
      background-color: #111;
      box-shadow: 0 0 10px var(--accent);
    }

    p {
      margin-top: 1rem;
      color: #a9cfe3;
    }

    a {
      color: var(--accent);
      text-decoration: none;
    }

    a:hover {
      text-shadow: 0 0 5px var(--accent);
    }
  </style>
</head>
<body>
  <div class="card">
    <img src="../secure-icon.png" alt="icon" />
    <h2>Login</h2>

    <form action="/auth/login" method="POST">
      <label for="email">Email</label>
      <input type="email" id="email" name="email" placeholder="Enter Email" />

      <label for="password">Password</label>
      <input type="password" id="password" name="password" placeholder="Enter Password" />

      <button type="submit" class="btn-primary">Login</button>
    </form>

    <h5 style="margin: 1.5rem 0;">OR</h5>

    <form action="https://afe4-2001-569-5988-8700-18f6-293d-6c87-60c.ngrok-free.app/auth/github" method="GET">
      <button type="submit" class="btn-dark">
        <i class="fab fa-github"></i> Login with GitHub
      </button>
    </form>

    <p>New User? <a href="/auth/register">Register</a></p>
    <p>Forgot Password? <a href="/auth/forgot">Reset</a></p>
  </div>
</body>
</html>
